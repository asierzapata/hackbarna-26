[Unit]
Description=Deploy kan room-server from origin/main
Wants=network-online.target
After=network-online.target docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/kan-deploy
# A build plus the health wait; well clear of the worst case, still bounded.
TimeoutStartSec=900
