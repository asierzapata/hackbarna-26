[package]
name = "kan"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "kan_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[features]
# Embeds a W3C WebDriver server (127.0.0.1:4445) inside the app so the real
# WKWebView can be driven in tests. Off by default and never enabled for a
# release bundle — `npm run tauri:drive` turns it on for local dev.
webdriver = ["dep:tauri-plugin-webdriver"]

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-webdriver = { version = "0.2", optional = true }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
getrandom = "=0.3.4"


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
